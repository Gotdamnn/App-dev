<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>PatientPulse Admin Login</title>
	<link rel="stylesheet" href="/css/login.css">
	<link href="https://fonts.googleapis.com/css?family=Inter:400,500,600,700&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
	<div class="login-wrapper">
		<div class="login-container">
			<div class="logo-section">
				<div class="logo-icon">P</div>
				<h1 class="logo-text">PatientPulse</h1>
			</div>
			<h2>Welcome Back</h2>
			<p class="subtitle">Sign in to your admin dashboard</p>
			
			<form id="login-form">
				<div class="form-group">
					<label for="email">Email Address</label>
					<input type="email" id="email" name="email" placeholder="admin@patientpulse.com" required>
				</div>
				
				<div class="form-group">
					<label for="password">Password</label>
					<input type="password" id="password" name="password" placeholder="Enter your password" required>
				</div>
				
				<div class="form-options">
					<label class="remember-me">
						<input type="checkbox" name="remember">
						<span>Remember me</span>
					</label>
					<a href="#" class="forgot-password">Forgot password?</a>
				</div>
				
				<button type="submit" class="login-btn">Sign In</button>
			</form>
			
			<div class="footer-links">
				<a href="#" class="footer-link"><i class="fas fa-headset"></i> Contact Support</a>
				<a href="#" class="footer-link"><i class="fas fa-circle"></i> System Status</a>
			</div>
		</div>
		
		<div class="login-footer">
			<p>&copy; 2026 PatientPulse Healthcare Systems | Secure • HIPAA Compliant • Encrypted</p>
		</div>
	</div>

	<!-- Error Modal -->
	<div id="errorModal" class="modal-overlay" style="display: none;">
		<div class="modal-content">
			<div class="modal-icon error">
				<i class="fas fa-exclamation-circle"></i>
			</div>
			<h3 id="modalTitle">Login Failed</h3>
			<p id="modalMessage">Please check your credentials and try again.</p>
			<button class="modal-btn" onclick="closeModal()">OK</button>
		</div>
	</div>

	<style>
		.modal-overlay {
			position: fixed;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background: rgba(0, 0, 0, 0.6);
			display: flex;
			align-items: center;
			justify-content: center;
			z-index: 9999;
			backdrop-filter: blur(4px);
		}
		.modal-content {
			background: #1e293b;
			border-radius: 16px;
			padding: 32px;
			text-align: center;
			max-width: 400px;
			width: 90%;
			box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
			border: 1px solid rgba(255, 255, 255, 0.1);
			animation: modalSlideIn 0.3s ease;
		}
		@keyframes modalSlideIn {
			from { transform: translateY(-20px); opacity: 0; }
			to { transform: translateY(0); opacity: 1; }
		}
		.modal-icon {
			width: 64px;
			height: 64px;
			border-radius: 50%;
			display: flex;
			align-items: center;
			justify-content: center;
			margin: 0 auto 20px;
			font-size: 28px;
		}
		.modal-icon.error {
			background: rgba(239, 68, 68, 0.2);
			color: #ef4444;
		}
		.modal-icon.success {
			background: rgba(34, 197, 94, 0.2);
			color: #22c55e;
		}
		.modal-content h3 {
			color: #fff;
			margin: 0 0 12px;
			font-size: 20px;
		}
		.modal-content p {
			color: #94a3b8;
			margin: 0 0 24px;
			font-size: 14px;
		}
		.modal-btn {
			background: #3b82f6;
			color: #fff;
			border: none;
			padding: 12px 48px;
			border-radius: 8px;
			font-size: 14px;
			font-weight: 600;
			cursor: pointer;
			transition: background 0.2s;
		}
		.modal-btn:hover {
			background: #2563eb;
		}
	</style>
	
	<script src="/js/login.js"></script>
</body>
</html>
